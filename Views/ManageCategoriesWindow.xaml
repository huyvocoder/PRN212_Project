<Window x:Class="PRN212_Project.Views.ManageCategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRN212_Project.Views"
        xmlns:vm="clr-namespace:PRN212_Project.ViewModels"
        mc:Ignorable="d"
        Title="Quản lý danh mục" Height="400" Width="500">

    <Grid Margin="10">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding NewCategoryName}" Width="200" Margin="0,0,5,0"/>
                <TextBox Text="{Binding NewCategoryLimit, StringFormat=C}" Width="100" Margin="0,0,5,0"/>
                <Button Content="Thêm" Command="{Binding AddCategoryCommand}" Width="100" Margin="5"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Categories}" AutoGenerateColumns="False" Height="250" Margin="0,10,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Định mức" Binding="{Binding MonthlyLimit, StringFormat=C}"/>
                    <DataGridTemplateColumn Header="Hành động">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Sửa" Command="{Binding DataContext.EditCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Id}" Width="50" Margin="5"/>
                                    <Button Content="Xóa" Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Id}" Width="50" Margin="5"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Lưu" Command="{Binding SaveCommand}" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>